/**
 * Forgot password form - just has the email and button to send reset password link
 */

const ForgotPassword = () => {
  return (
    <div>
      Forgot password - just has input and a button
    </div>
  )
}

export default ForgotPassword
