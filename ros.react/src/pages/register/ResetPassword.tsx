/**
 * Form that picks up the email and token and query params and then has form inputs to reset the PW
 */

const ResetPassword = () => {
  return (
    <div>
      Get query param reset password form
    </div>
  )
}

export default ResetPassword
